# 项目启动指南

## 项目结构说明

本项目包含两个部分：
1. **后端服务** (`reading-cards-backend/`) - 提供 API 服务和前端页面
2. **前端扩展** (根目录) - Chrome 扩展（可选，用于开发）

---

## 第一步：启动后端服务

### 1.1 进入后端目录

```bash
cd reading-cards-backend
```

### 1.2 安装依赖（如果还没安装）

```bash
npm install
```

### 1.3 配置环境变量（可选）

如果需要使用 AI 功能，创建 `.env` 文件：

```bash
# 在 reading-cards-backend 目录下创建 .env 文件
# Windows PowerShell:
New-Item -Path .env -ItemType File

# 然后编辑 .env 文件，添加：
# OPENAI_API_KEY=your_api_key_here
# PORT=3000
```

**注意**：如果没有 `.env` 文件，服务也能正常启动，只是 AI 功能不可用。

### 1.4 启动后端服务

```bash
npm run dev
```

或者：

```bash
npm start
```

### 1.5 验证服务是否启动成功

看到以下输出表示启动成功：

```
🚀 服务器运行在 http://localhost:3000
📄 前端页面: http://localhost:3000
🔍 健康检查: http://localhost:3000/health
```

### 1.6 访问前端页面

在浏览器中打开：**http://localhost:3000**

---

## 第二步：开发前端扩展（可选）

如果需要开发或构建 Chrome 扩展：

### 2.1 返回项目根目录

```bash
cd ..
```

### 2.2 安装依赖（如果还没安装）

```bash
npm install
```

### 2.3 启动开发服务器

```bash
npm run dev
```

### 2.4 构建扩展

```bash
npm run build
```

构建完成后，扩展文件在 `dist/` 目录。

---

## 常见问题

### Q: 端口 3000 被占用怎么办？

A: 修改 `reading-cards-backend/.env` 文件，设置其他端口：
```
PORT=3001
```

### Q: 如何检查后端服务是否正常运行？

A: 访问健康检查接口：http://localhost:3000/health

应该返回：`{"ok":true}`

### Q: 依赖安装失败怎么办？

A: 
1. 删除 `node_modules` 文件夹
2. 删除 `package-lock.json` 文件
3. 重新运行 `npm install`

### Q: 需要同时启动后端和前端扩展吗？

A: 不需要。如果只是使用卡片管理功能，只需要启动后端服务即可。前端扩展主要用于开发 Chrome 扩展功能。

---

## 快速启动命令总结

**仅启动后端（推荐）：**
```bash
cd reading-cards-backend
npm install  # 首次运行需要
npm run dev
```

**开发前端扩展：**
```bash
npm install  # 首次运行需要
npm run dev
```






<<<<<<< HEAD
=======








>>>>>>> d68f0a4 (docs: update documentation, guides and project config)
